export default () => {
    switch(action.type) {
        case "FETCH_ALL":
            return services;
        case "FETCH_ONE":
            return services;
        case "CREATE":
            return services;
        default:
            return services;
    }
}